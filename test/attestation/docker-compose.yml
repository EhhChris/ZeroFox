services:
  haproxy:
    build: ./haproxy
    ports:
      - "8080:80"
    volumes:
      - ../../build/haproxy-private.pem:/etc/haproxy/private.pem:ro
    depends_on:
      - httpd

  httpd:
    image: httpd:2.4-alpine
